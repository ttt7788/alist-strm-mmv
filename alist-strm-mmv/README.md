Alist-Strm (Pro 增强版) 基于 WebDAV 的高性能 STRM 媒体链接生成与同步工具。 本项目在原版（Brian099/alist-strmhttps://github.com/Brian099/alist-strm）强大的核心功能基础上，进行了全方位的 UI 现代化重构与底层多线程性能压榨，专为追求极致视觉体验与极速同步的媒体库玩家打造。

✨ 为什么选择这个增强版？ 相比原版，本增强版带来了以下颠覆性升级：

🎨 极致美学 UI：全站采用现代「毛玻璃 (Glassmorphism)」质感设计，重绘动态渐变图标与全套 Bootstrap Icons，告别传统后台的沉闷。

⚡️ 多线程极速扫描：重构底层遍历逻辑，加入多线程并发目录扫描 (BFS)。云端比对速度飙升，增量更新秒级完成，大幅降低网络 I/O 阻塞。

📊 实时任务大屏：首页新增动态进度监控面板，后台同步进度、扫描状态一目了然，无需频繁点开日志。

🤖 全自动构建：集成 GitHub Actions，代码推送即自动构建 Docker 镜像并发布至 Docker Hub，支持无缝对接各大 NAS 系统。

💡 核心功能 全/增量智能同步：自动比对本地与 WebDAV 云端文件，仅生成缺失的 .strm 文件，0 变动时瞬间结束任务。

多配置管理：支持同时绑定多个 Alist / WebDAV 节点，分别映射不同的本地目录。

Cron 定时任务：内置强大的定时调度器（支持按分钟、小时、天、周调度），实现完全无人值守的全自动追更同步。

自动化清理 (防拉跨)：自动检测并一键清理本地残留的失效 STRM 文件（云端已删除的资源）。

数据库日志系统：告别海量文本日志导致的磁盘读写卡顿，采用轻量级 SQLite 存储运行日志。

实用工具箱：内置“批量替换域名”等高级工具，方便 Alist 服务器迁移。

🛠️ 安装与部署 本项目已全面容器化，推荐使用 Docker / Docker Compose 进行一键部署。完美支持群晖、威联通、Unraid 以及 飞牛 OS (fnOS)。

方式一：使用 Docker Compose (推荐) 在你的服务器或 NAS 上创建一个空文件夹，在其中新建 docker-compose.yml 文件，填入以下内容：

然后执行启动命令：

方式二：飞牛 OS (fnOS) 专属安装 如果你使用的是飞牛私有云，可以通过以下步骤获得原生 App 体验：

确保已在飞牛底层安装 fnpackup 脚手架。

填入本镜像 akjehsmhq5/alist-strm:latest，映射对应的 /config 和 /data 目录。

一键打包为 .fpk 文件后，直接在飞牛“应用中心”手动安装，即可在桌面生成快捷图标。

📖 快速上手指南 访问后台：浏览器打开 http://你的IP:5000。

注册登录：首次运行需要注册一个管理员账号（所有数据均保存在本地 SQLite 中，绝对安全）。

添加配置：进入「配置文件」菜单，填写你的 Alist WebDAV 地址、账号密码，以及对应的根目录（如 /dav/电影）和本地映射目录（如 /data/电影）。

测试运行：在配置列表勾选刚刚创建的配置，点击「运行选中」，去首页查看实时扫描进度。

设置定时：进入「定时任务」菜单，为该配置绑定一个执行频率（如每 2 小时执行一次增量同步）。

挂载媒体库：将宿主机的 ./data 目录挂载到 Emby / Jellyfin / Plex 中进行刮削即可！

🤝 鸣谢与声明 本项目核心同步逻辑基于 深度优化与重构，特此感谢原作者的开源贡献。

仅供学习与技术交流使用，请勿用于商业用途。（代码全部由GeminiPRO生成）